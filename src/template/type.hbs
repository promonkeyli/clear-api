declare namespace API {
{{#each typeData }}
    type {{this.name}} = {
      {{#each this.properties}}
          {{#if this.description}}
          /* {{this.description}} */
          {{/if}}
          {{this.name}}?: {{this.type}};
      {{/each}}
    }
{{/each}}
}
